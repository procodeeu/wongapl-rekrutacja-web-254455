version: '3.8'
services:
  app:
    build: .
    ports:
      - "4321:4321"
      - "4001:4001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - TINACMS_DEV_HOST=0.0.0.0
    stdin_open: true
    tty: true